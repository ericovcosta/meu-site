1ª vez que usa o repositório
1. git init
	cria um repositório vazio

2. git add .
	adiciona todos os arquivos modificados ao commit

5. git branch -M main
	cria um "ramo" main no git

4. git commit -m "meu primeiro commit"**
	cria o commit com a mensagem "meu primeiro commit"
      **git config user.email "o email do github"
	git config user.name "o nome do usuario"

3. git remote add origin https://github.com/<usuario>/<nome do repositorio>.git
	adiciona uma origem "origin" no repositorio local

6. git push -u origin main*
	empurra o repositório local para o remoto

sudo snap install gh
gh auth login

https://fontawesome.com/